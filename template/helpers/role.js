define('template/helpers/role', ['Handlebars'], function (Handlebars) {
    var role = function (resultTotal, currentPage, perPage, maxPaginationLinks, next, prev, options) {
    	if ()
    	return "";
    };

    Handlebars.registerHelper('role', role);

    return role;
});
